# 参与贡献

感谢你改进载衡。提交代码前请先确认改动不包含真实员工、项目、工时、备份、登录背景照片、生产域名或访问凭据。

## 开发流程

1. 从主分支创建短生命周期分支。
2. 保持改动聚焦，并为业务规则或缺陷补充测试。
3. 执行：

   ```bash
   gofmt -w .
   go mod tidy
   go vet ./...
   go test ./... -count=1
   go build -buildvcs=false -trimpath .
   ```

4. 若修改依赖或工具链，再运行 `govulncheck ./...`。
5. 在拉取请求中说明业务口径、权限影响、数据库兼容性和验证方法。

## 代码约定

- 保持标准 Go 风格，提交前运行 `gofmt`。
- SQL 必须使用参数绑定；动态表名或列名只能来自代码内固定白名单。
- 新增 POST 路由必须经过认证、权限和 CSRF 校验。
- 新增管理员功能必须写入操作审计。
- 数据库变化必须兼容已有数据库，不得依赖删除生产数据。
- 模板不得把未经编码的用户输入转换为 `template.HTML` 或 `template.JS`。
- 测试数据只能使用 `example.com`、`test.local` 和明显虚构的姓名、电话、项目。

## Issue 与拉取请求

缺陷报告应包含复现步骤、预期行为、实际行为、使用角色和环境。请先遮盖截图中的姓名、邮箱、项目编号和工时。安全漏洞遵循 [SECURITY.md](SECURITY.md)，不要放在公开 Issue 中。

## 贡献许可

提交代码、文档或其他内容，即表示你确认有权提交这些内容，并同意其按本项目的 `AGPL-3.0-only` 许可证提供。请勿提交来源不明、许可证不兼容或属于第三方保密资料的内容。
