# 安全策略

## 支持范围

项目正式发布版本后，只维护最新稳定版本。当前仍处于首次开源准备阶段，安全修复优先进入主分支。

## 报告漏洞

请使用 GitHub 仓库的“Private vulnerability reporting”或 Security Advisory 私密报告。不要在公开 Issue、讨论区或拉取请求中披露未修复漏洞。

报告建议包含：

- 受影响版本或提交；
- 复现条件和最小复现；
- 可能影响的数据或权限；
- 建议修复方向；
- 是否已在真实系统验证。

请勿访问、下载或修改不属于你的真实员工、项目、工时、备份或账号数据。

## 部署安全基线

- 使用 Go 1.26.6 或更高安全补丁版本构建。
- 应用仅监听回环地址，由 Caddy 或等效反向代理终止 HTTPS。
- 生产环境设置 `APP_COOKIE_SECURE=true`。
- 数据库、环境文件、备份和云盘 OAuth 配置只允许服务账号读取。
- SSH 使用密钥认证，防火墙仅开放必要端口。
- 定期运行 `govulncheck ./...` 并及时升级 Go 工具链。
- 恢复备份前先生成保护备份，并在恢复后重新核对账号与权限。

公开的安全策略不替代部署单位自己的合规、隐私和备份制度。
